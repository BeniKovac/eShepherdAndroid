<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eshepherd">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EShepherd">
        <activity android:name=".AddJagenjcekActivity"></activity>
        <activity android:name=".AddOvenActivity" />
        <activity android:name=".AddKotitevActivity" />
        <activity android:name=".AddGonitevActivity" />
        <activity android:name=".AddCredaActivity" />
        <activity android:name=".AddOvcaActivity" />
        <activity android:name=".OvceDisplayActivity" />
        <activity android:name=".EntityDisplaysActivity" />
        <activity android:name=".AddingActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>